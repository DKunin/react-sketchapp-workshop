import React from 'react';
import { View } from 'react-primitives';

export default () => (
    <View
        name='Separator'
        style={{
            paddingTop: 10,
            paddingBottom: 10
        }} />
);
