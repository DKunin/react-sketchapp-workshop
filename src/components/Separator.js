import React from 'react';
import PropTypes from 'prop-types';
import { View } from 'react-sketchapp';

export default () => (
  <View
    name="Separator"
    style={{
      paddingTop: 10,
      paddingBottom: 10
    }}
  />
);